# Git remote for publishing this distribution.
# Edit REMOTE before first publish.

REMOTE=git@github.com:research-limingjun/reversesearchdev.git
BRANCH=main

# Conflict handling: distribution_branch (auto push conflict branch) | alert_only (text only)
CONFLICT_STRATEGY=distribution_branch
CONFLICT_BRANCH_PREFIX=distribution/Conflict
